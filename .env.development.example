# Copia este archivo como .env.development.local para pruebas locales.
# El archivo .local queda ignorado por Git y no afecta los builds de producción.
VITE_API_BASE_URL=http://localhost:3000
VITE_API_PROXY_TARGET=http://localhost:3000
